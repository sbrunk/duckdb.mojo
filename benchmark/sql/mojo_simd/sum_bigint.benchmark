# name: benchmark/micro/mojo_simd/sum_bigint.benchmark
# group: [mojo_simd]

name SUM(bigint)
group micro
subgroup mojo_simd

load
CREATE TABLE t AS SELECT ((i % 1000) + 1)::BIGINT AS x FROM range(50000000) tbl(i);

run
SELECT SUM(x) FROM t;
