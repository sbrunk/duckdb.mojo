# name: benchmark/micro/mojo_simd/avg_hugeint.benchmark
# group: [mojo_simd]

name AVG(hugeint)
group micro
subgroup mojo_simd

load
CREATE TABLE t AS SELECT (((i % 1000) + 1))::HUGEINT AS x FROM range(50000000) tbl(i);

run
SELECT AVG(x) FROM t;
