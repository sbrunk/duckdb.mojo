# name: benchmark/micro/mojo_simd/min_float.benchmark
# group: [mojo_simd]

name min_float
group micro
subgroup mojo_simd

load
CREATE TABLE t AS SELECT (((i*2654435761) % 1000000))::FLOAT AS x FROM range(50000000) tbl(i);

run
SELECT MIN(x) FROM t;
