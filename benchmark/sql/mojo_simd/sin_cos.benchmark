# name: benchmark/micro/mojo_simd/sin_cos.benchmark
# description: Mojo SIMD patch target — sin(x)+cos(x)
# group: [mojo_simd]

name sin(x)+cos(x)
group micro
subgroup mojo_simd

load
CREATE TABLE t AS SELECT ((i % 1000) + 1)::DOUBLE AS x, (((i*7) % 1000) + 1)::DOUBLE AS y FROM range(50000000) tbl(i);

run
SELECT SUM(sin(x)+cos(x)) FROM t;
