# name: benchmark/micro/mojo_simd/sum_decimal38.benchmark
# group: [mojo_simd]

name SUM(decimal38)
group micro
subgroup mojo_simd

load
CREATE TABLE t AS SELECT (((i % 1000) + 1) * 1.01)::DECIMAL(38,4) AS x FROM range(50000000) tbl(i);

run
SELECT SUM(x) FROM t;
